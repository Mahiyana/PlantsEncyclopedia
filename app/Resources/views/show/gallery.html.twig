<h1>{{ gallery.name }}</h1>
<h3>{{ gallery.description }}</h3>
{% for image in gallery.images %}
<table>
  <tr>
    <th>
      {{ image.name }}</br>
      <a href="/show/image/{{image.id}}"><img src="{{ asset('images/' ~ image.getFullSize) }}" width="80px"/></a>
    </th>
  </tr>
</table>
{% endfor %}


<a href='/add/image'>Dodaj nowy obrazek </a></br>
<a href='/show/galleries'>Powrót do listy galerii </a>
